This is a demo folder containing executable scripts and test datasets.
If you accidentally lose or corrupt some files, you can re-download it from GitHub,
or run `git pull` on `ezwgd` folder (if you cloned this source code using `git clone`).
